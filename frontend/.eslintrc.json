{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "Literal[raw=/^#[0-9a-fA-F]{3,6}$/]",
        "message": "Hardcoded hex colors are not allowed. Use theme tokens like var(--theme-primary) instead."
      },
      {
        "selector": "TemplateLiteral[expressions.length=0][quasis.0.value.raw=/^#[0-9a-fA-F]{3,6}$/]",
        "message": "Hardcoded hex colors in template literals are not allowed. Use theme tokens instead."
      }
    ]
  }
}